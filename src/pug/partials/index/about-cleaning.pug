section.about-cleaning
  .container.about-cleaning__container
    .row.row_margin-xsm.about-cleaning__row

      h2.title.title__secondary Про уборки

      p.about-cleaning__text Клинер приедет со всем необходимым для уборки. Стоимость уборки рассчитывается от количества комнат.

  .about-cleaning__slider

    #slider-cleaning.slider-cleaning(data-slider)

      .slider-cleaning__header В каждый тариф входит уборка:

        ul.slider-cleaning__indicators(data-slider-indicators="slider-cleaning")

          each item, index in sliderCleaning
            li.slider-cleaning__indicator(data-slide-to = index) #{item.name}

      ul.slider-cleaning__items(data-slider-items)

        each item, index in sliderCleaning
          li(data-slider-slide)
            figure.image-map
              +image(false, item.img, item.name, 'image-map__img')

              each mapItem, index in item.map
                i(class="image-map__marker image-map__marker_" + mapItem.type style = mapItem.style data-tooltip-content = mapItem.tpCont data-tooltip-position = mapItem.tpPos)
